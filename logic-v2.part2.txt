ают условия.'},different:{ja:'制度や技術が違えば結果も変わります。',ru:'Другие институты могут дать другой результат.'},caution:{ja:'似た構造は同じ結果を意味しません。',ru:'Сходство не означает одинаковый результат.'}},sapiens:[],test:{question:{ja:`${l(p.title)}が重要な理由を説明してください。`,ru:`Почему ${l(p.title)} важно?`}},evidence:[]}}
const E=id=>D.EVENTS?.[id]||generic(id);
function route(){const h=(location.hash||'#home').slice(1);if(h.startsWith('event/'))return{screen:'event',id:decodeURIComponent(h.slice(6))};return{screen:['home','timeline','library','map','profile'].includes(h)?h:'home'}}
function go(x){mem[route().screen]=scrollY;location.hash='#'+x}
function setIndex(i){idx=Math.max(0,Math.min(D.TIMEPOINTS.length-1,i));st.s('chronos-point',P().id);renderDial()}
function setTheme(v){theme=v;document.documentElement.dataset.theme=v;st.s('chronos-theme',v);$$('[data-theme-choice]').forEach(b=>b.classList.toggle('active',b.dataset.themeChoice===v))}
function setLang(v){lang=v;document.documentElement.lang=v;document.documentElement.dataset.lang=v;st.s('chronos-lang',v);renderText();renderAll();$$('.lang-code').forEach(x=>x.textContent=lang==='ja'?'RU':'JA')}
function setDepth(v){depth=v;st.s('chronos-depth',v);renderSettings()}
function renderText(){const t=T();$('#brandSub').textContent=t.brand;$('#homeTitle').innerHTML=`<span>${t.h1}</span><span>${t.h2}</span>`;$('#homeCopy').textContent=t.copy;$('#startLabel').textContent=t.start;$('#dialRange').textContent=t.range;$('#dialHint').textContent=t.hint;$('#continueTitle').textContent=t.cont;$('#resumeEvent').textContent=lang==='ja'?'フランス革命':'Французская революция';$('#resumeCopy').textContent=lang==='ja'?'財政・思想・食料価格が、なぜ同じ瞬間に政治変動へつながったのか。':'Как финансы, идеи и цены на хлеб одновременно превратились в политический перелом.';$('#discoveryTitle').textContent=t.disc;$('#discoveryCardTitle').textContent=lang==='ja'?'活版印刷 × 宗教改革 × 情報の暴走':'Печатный станок × Реформация × информационные волны';$('#discoveryCardCopy').textContent=lang==='ja'?'印刷革命と現代のSNSは、どこが似ていて、どこが違う？':'Чем печатная революция похожа на соцсети — и чем отличается?';$('#timelineTitle').textContent=t.timeline;$('#timelineCopy').textContent=t.tlcopy;$('#prehumanLabel').textContent=t.prehuman;$('#scrollHint').textContent=t.scroll;$('#tapHint').textContent=t.tap;$('#mapTitle').textContent=t.map;$('#backLabel').textContent=t.back;['tabStory','tabConnections','tabWorld','tabMeaning'].forEach((id,i)=>$('#'+id).textContent=t.tabs[i]);$('#meaningTitle').textContent=t.meaning;$('#testTitle').textContent=t.test;$('#submitTestLabel').textContent=t.submit;$('#sourcesLabel').textContent=t.sources;$('#libraryTitle').textContent=t.library;$('#libraryCopy').textContent=t.libcopy;$('#myMapTitle').textContent=t.mymap;$('#myMapCopy').textContent=t.mapcopy;$('#settingsTitle').textContent=t.settings;$('#languageTitle').textContent=t.language;$('#themeTitle').textContent=t.theme;$('#depthTitle').textContent=t.depth;$('#depthCopy').textContent=t.depthcopy;$('#guideTitle').textContent=t.guide;$('#guideCopy').textContent=t.guidecopy;$('[data-action="guide"]#x');const ids=['navHome','navTimeline','navLibrary','navMap','navProfile'];ids.forEach((id,i)=>$('#'+id).textContent=t.nav[i]);['menuTimeline','menuMap','menuGuide','menuSettings','menuClose'].forEach((id,i)=>$('#'+id).textContent=t.menu[i]);$('#searchInput').placeholder=t.search;$$('.lang-code').forEach(x=>x.textContent=lang==='ja'?'RU':'JA')}
function applyRoute(){const r=route();$$('.screen').forEach(s=>s.classList.toggle('active',s.dataset.screen===r.screen));$$('.nav-button').forEach(b=>b.classList.toggle('active',b.dataset.route===r.screen));$('#topbar').hidden=r.screen==='home';if(r.screen==='event')renderEvent(r.id);if(r.screen==='timeline')setTimeout(()=>scrollToIndex(idx,false),20);if(r.screen==='library')renderLibrary();if(r.screen==='map')renderMyMap();if(r.screen==='profile')renderSettings();closeSheets();requestAnimationFrame(()=>scrollTo(0,mem[r.screen]||0))}
function renderDial(){const max=Math.max(1,(D.TIMEPOINTS.length-1)*10);const sl=$('#homeTimeSlider');sl.max=max;sl.value=idx*10;$('#dialYear').textContent=l(P().label);$('#dialEvent').textContent=l(P().title);const picks=[0,2,6,8,10,14,D.TIMEPOINTS.length-1].filter((x,i,a)=>x<D.TIMEPOINTS.length&&a.indexOf(x)===i);$('#dialTicks').innerHTML=picks.map(i=>`<button class="dial-tick ${i===idx?'active':''}" data-dial-index="${i}">${short(P(i))}</button>`).join('')}
function short(p){if(!p)return'';if(p.id==='sapiens')return lang==='ja'?'30万年前':'300k';if(p.id==='agriculture')return lang==='ja'?'1万年前':'10k';return l(p.label).replace('頃','').replace('ок. ','')}
function renderFilters(){const cats=T().cats;$('#timelineFilters').innerHTML=[['all',T().all],...Object.entries(cats)].map(([k,v])=>`<button class="filter-button ${filter===k?'active':''}" data-timeline-filter="${k}">${v}</button>`).join('');$('#libraryFilters').innerHTML=[['all',T().all],['human',cats.human],['power',cats.power],['ideas',cats.ideas],['tech',cats.tech]].map(([k,v])=>`<button class="filter-button ${libFilter===k?'active':''}" data-library-filter="${k}">${v}</button>`).join('')}
const lane=[92,198,304,410];
function renderTimeline(){const canvas=$('#historyCanvas'),spacing=246,start=128,w=Math.max(innerWidth-40,start*2+spacing*(D.TIMEPOINTS.length-1));canvas.style.width=w+'px';let h='';lane.forEach((y,i)=>h+=`<div class="lane-line" style="top:${y}px"></div><div class="lane-name" style="top:${y}px">${lang==='ja'?['人類・環境','政治・帝国','思想・宗教','技術・経済'][i]:['Люди и среда','Политика и империи','Идеи и религии','Технологии и экономика'][i]}</div>`);D.TIMEPOINTS.forEach((p,i)=>{if(filter!=='all'&&p.cat!==filter)return;h+=`<button class="timeline-event ${i===idx?'focused':''}" data-point-index="${i}" style="left:${start+i*spacing}px;top:${lane[p.lane??0]}px"><small>${esc(l(p.label))}</small><strong>${esc(l(p.title))}</strong><span>${esc(p.region)}</span></button>`});canvas.innerHTML=h;$('#timelineNowYear').textContent=l(P().label);$('#timelineNowLabel').textContent=l(P().title)}
function scrollToIndex(i,smooth=true){setIndex(i);const sc=$('#historyScroll'),left=128+i*246-sc.clientWidth/2;sc.scrollTo({left:Math.max(0,left),behavior:smooth?'smooth':'auto'});renderTimeline();renderMap(P().id,'timelineWorldMap');renderMapContext()}
function onScroll(){cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{const sc=$('#historyScroll'),i=Math.max(0,Math.min(D.TIMEPOINTS.length-1,Math.round((sc.scrollLeft+sc.clientWidth/2-128)/246)));if(i!==idx){setIndex(i);$('#timelineNowYear').textContent=l(P().label);$('#timelineNowLabel').textContent=l(P().title);$$('.timeline-event').forEach((b,j)=>b.classList.toggle('focused',j===i));renderMap(P().id,'timelineWorldMap');renderMapContext()}})}
function facts(e){const a=e.facts||[];if(a.length&&a[0]?.label)return a.map(x=>`<div class="fact"><small>${esc(l(x.label))}</small><strong>${esc(l(x.value))}</strong></div>`).join('');let h='';for(let i=0;i<a.length;i+=2)h+=`<div class="fact"><small>${esc(l(a[i]))}</small><strong>${esc(l(a[i+1]))}</strong></div>`;return h}
function renderEvent(id){const e=E(id),p=pBy(e.point)||P();if(p){idx=Math.max(0,D.TIMEPOINTS.indexOf(p));st.s('chronos-point',p.id)}$('#eventKicker').textContent=`${l(e.period||p?.label)} · ${l(e.region||p?.region)}`;$('#eventTitle').textContent=l(e.title);$('#eventLead').textContent=l(e.lead);const im=$('#eventImage'),src=imageFor(id)||imageFor(p?.id);im.src=src||fallback(l(e.title));im.alt=l(e.title);im.onerror=()=>{im.onerror=null;im.src=fallback(l(e.title))};$('#storyHeadline').textContent=l(e.storyHeadline);const story=l(e.story);$('#storyBody').innerHTML=(Array.isArray(story)?story:[story]).filter(Boolean).map(x=>`<p>${esc(x)}</p>`).join('');$('#eventFacts').innerHTML=facts(e);renderConnections(e);renderSame(e);renderMeaning(e,'deep');$('#testQuestion').textContent=l(e.test?.question)|| (lang==='ja'?'この出来事が重要な理由を、自分の言葉で説明してください。':'Объясните своими словами, почему это событие важно.');$('#testAnswer').value='';$('#testFeedback').hidden=true;$('#evidenceBody').innerHTML=(e.evidence||[]).map(x=>`<p>${esc(l(x.text||x))}</p>`).join('')||`<p>${lang==='ja'?'出典情報を整備中です。':'Источники дополняются.'}</p>`;renderMap(p?.id||P().id,'eventWorldMap');$$('.event-tab').forEach((x,i)=>x.classList.toggle('active',i===0));$$('.event-panel').forEach((x,i)=>x.classList.toggle('active',i===0));mode='deep'}
function renderConnections(e){$('#causeGrid').innerHTML=(e.causes||[]).map(c=>`<article class="cause-card"><small>${esc(l(c.type||c.label||''))}</small><strong>${esc(l(c.title))}</strong><p>${esc(l(c.text||c.copy||''))}</p></article>`).join('')||`<p class="page-copy">${lang==='ja'?'複合原因を整理中です。':'Причинные связи дополняются.'}</p>`;$('#convergence').textContent=`↓ ${l(e.title)} ↓`;$('#outcomeGrid').innerHTML=(e.outcomes||[]).map(o=>`<article class="outcome-card"><strong>${esc(l(o.title))}</strong><p>${esc(l(o.text||o.copy||''))}</p></article>`).join('');$('#connectionGraph').innerHTML=`<div style="padding:22px;color:var(--muted)">${lang==='ja'?'原因 → 出来事 → 結果 → 長期影響を、関連データが増えるほど広げます。':'Причины → событие → последствия → долгосрочное влияние.'}</div>`}
function renderSame(e){const a=e.sameTime||[];$('#sameTimeGrid').innerHTML=a.length?a.map((x,i)=>{const src=imageFor(x.id||'')||fallback(l(x.title));return`<article class="same-card"><img src="${src}" onerror="this.onerror=null;this.src='${fallback('CHRONOS')}'"><div class="same-card-copy"><small>${esc(l(x.region||''))}</small><strong>${esc(l(x.title))}</strong><p>${esc(l(x.text||x.copy||''))}</p></div></article>`}).join(''):`<p class="page-copy">${lang==='ja'?'同時代比較を追加中です。':'Сравнение с другими регионами дополняется.'}</p>`}
function renderMeaning(e,m){mode=m;$$('.meaning-tab').forEach(b=>b.classList.toggle('active',b.dataset.meaning===m));const out=$('#meaningContent');if(m==='deep'){const a=e.deep||[];out.innerHTML=`<div class="meaning-stack">${a.length?a.map(x=>`<article class="deep-card"><small>${esc(l(x.label||x.type||''))}</small><strong>${esc(l(x.title))}</strong><p>${esc(l(x.text||x.copy||''))}</p></article>`).join(''):`<article class="deep-card"><strong>${lang==='ja'?'別分野からの深掘りを追加中':'Междисциплинарные связи дополняются'}</strong></article>`}</div>`}else if(m==='echoes'){const x=e.echoes||{};out.innerHTML=`<div class="meaning-stack"><article class="echo-card"><small>STRUCTURE</small><strong>${esc(l(x.structure||''))}</strong></article><article class="echo-card"><strong>${lang==='ja'?'似ている構造':'Что похоже'}</strong><p>${esc(l(x.same||''))}</p></article><article class="echo-card"><strong>${lang==='ja'?'違う条件':'Что отличается'}</strong><p>${esc(l(x.different||''))}</p></article><article class="echo-card"><strong>${lang==='ja'?'注意':'Ограничение'}</strong><p>${esc(l(x.caution||''))}</p></article></div>`}else{const a=e.sapiens||[];out.innerHTML=`<div class="meaning-stack">${a.length?a.map(x=>`<article class="sapiens-card"><small>HOMO SAPIENS PATTERN</small><strong>${esc(l(x.title))}</strong><p>${esc(l(x.text||x.copy||''))}</p></article>`).join(''):`<article class="sapiens-card"><strong>${lang==='ja'?'この出来事から言える人間行動の傾向を検討中':'Выводы о человеческом поведении дополняются'}</strong></article>`}</div>`}}
function submitTest(){const e=E(route().id||'french-revolution'),ans=$('#testAnswer').value.trim(),fb=$('#testFeedback');if(ans.length<18){fb.hidden=false;fb.innerHTML=`<h3>${lang==='ja'?'まず一つの原因から書いてみましょう':'Начните хотя бы с одной причины'}</h3><p>${lang==='ja'?'「○○が起きた。だから△△が変わった」の形でも十分です。':'Формата «произошло X, поэтому изменилось Y» уже достаточно.'}</p>`;return}const concepts=e.test?.concepts||[],low=ans.toLowerCase(),hit=concepts.filter(c=>(Array.isArray(c)?c:[c]).some(w=>low.includes(String(w).toLowerCase()))).length;const strong=ans.length>90||hit>=2;fb.hidden=false;fb.innerHTML=`<h3>${strong?(lang==='ja'?'複数の条件をつないで考えられています':'Вы связываете несколько условий'):(lang==='ja'?'因果関係を自分の言葉にできています':'Вы уже формулируете причинность своими словами')}</h3><p>${lang==='ja'?'良かった点：出来事を年号だけでなく「なぜ→何が変わった」で説明しようとしています。':'Сильная сторона: вы объясняете не дату, а связь «почему → что изменилось».'}</p><p><b>${lang==='ja'?'次の一歩':'Следующий шаг'}：</b> ${strong?(lang==='ja'?'同じ条件があったのに別の結果になった地域がないか考えてみてください。':'Сравните с регионом, где похожие условия дали другой результат.'):(lang==='ja'?'社会・経済・思想のうち、まだ触れていない観点を一つ足してみましょう。':'Добавьте ещё один ракурс: общество, экономика или идеи.')}</p>`;const m=new Set(st.j('chronos-mastered',[]));m.add(e.point||route().id);st.sj('chronos-mastered',[...m]);renderMyMap()}
function renderLibrary(){const arr=D.TIMEPOINTS.filter(p=>libFilter==='all'||p.cat===libFilter);$('#libraryGrid').innerHTML=arr.map(p=>{const id=eventForPoint(p.id),src=imageFor(id)||imageFor(p.id)||fallback(l(p.title));return`<button class="library-card" data-event="${esc(id)}"><img src="${src}" alt="${esc(l(p.title))}" loading="lazy"><div class="library-card-copy"><small>${esc(T().cats[p.cat]||p.cat)}</small><strong>${esc(l(p.title))}</strong><span>${esc(l(p.label))}</span></div></button>`}).join('');$$('.library-card img').forEach(im=>im.onerror=()=>{im.onerror=null;im.src=fallback(im.alt)})}
function renderMyMap(){const mastered=new Set(st.j('chronos-mastered',[])),visited=new Set(st.j('chronos-visited',[])),done=mastered.size,pct=Math.min(100,Math.round(done/50*100));$('#scorePercent').textContent=pct+'%';$('#understandingLabel').textContent=lang==='ja'?'説明できるつながり':'объяснимых связей';$('#scoreTitle').textContent=lang==='ja'?`説明できるテーマ ${done} / 50`:`Темы, которые могу объяснить: ${done} / 50`;$('#scoreCopy').textContent=lang==='ja'?'読んだ数ではなく、自分の言葉で因果を説明できたテーマを数えます。':'Мы считаем не просмотры, а темы, причинность которых вы можете объяснить.';$('.score-ring').style.setProperty('--score',pct+'%');const regions=[['Europe',0],['West Asia',0],['East Asia',0],['Africa',0],['South Asia',0],['Americas',0]];D.TIMEPOINTS.forEach(p=>{if(!visited.has(p.id))return;const r=p.region||'';if(/Europe|Britain|Mediterranean/.test(r))regions[0][1]++;else if(/East Asia/.test(r))regions[2][1]++;else if(/Africa/.test(r))regions[3][1]++;else if(/America|Atlantic/.test(r))regions[5][1]++;else if(/Asia|Arabia|Mesopotamia/.test(r))regions[1][1]++});$('#regionBars').innerHTML=regions.map(([n,c])=>{const v=Math.min(100,c*22);return`<div class="region-row"><span>${n}</span><div class="bar-track"><div class="bar-fill" style="width:${v}%"></div></div><strong>${v}%</strong></div>`}).join('');$('#mapInsightTitle').textContent=lang==='ja'?'知っている地域と、まだ霧の濃い地域が見えてきます':'Видно, где знания сильнее, а где ещё туман';$('#mapInsightCopy').textContent=lang==='ja'?'同じ時代の別地域へ寄り道すると、知っている出来事の意味が変わって見えます。':'Другой регион той же эпохи меняет взгляд на знакомое событие.';renderMap(P().id,'myWorldMap')}
function renderSettings(){$$('[data-lang-choice]').forEach(b=>b.classList.toggle('active',b.dataset.langChoice===lang));$$('[data-theme-choice]').forEach(b=>b.classList.toggle('active',b.dataset.themeChoice===theme));$$('[data-depth-choice]').forEach(b=>b.classList.toggle('active',b.dataset.depthChoice===depth));$('[data-depth-choice="auto"]').textContent=lang==='ja'?'自動':'Авто';$('[data-depth-choice="expert"]').textContent=lang==='ja'?'専門':'Эксперт'}
async function loadLand(){if(land)return land;if(!window.d3||!window.topojson)return null;try{const w=await d3.json('https://cdn.jsdelivr.net/npm/world-atlas@2/land-110m.json');return land=topojson.feature(w,w.objects.land)}catch{return null}}
function spots(id){const p=pBy(id)||P(),i=D.TIMEPOINTS.indexOf(p);return D.TIMEPOINTS.slice(Math.max(0,i-2),Math.min(D.TIMEPOINTS.length,i+3))}
async function renderMap(id,svgId){const svg=document.getElementById(svgId);if(!svg)return;const ld=await loadLand();if(ld&&window.d3){const pr=d3.geoNaturalEarth1().fitSize([930,470],ld),path=d3.geoPath(pr);let h=`<path class="graticule" d="${path(d3.geoGraticule10())}"></path><path class="land" d="${path(ld)}"></path>`;spots(id).forEach(p=>{const xy=pr([p.lon||0,p.lat||0]);if(xy)h+=`<g data-map-point="${p.id}"><circle class="hotspot-ring" cx="${xy[0]}" cy="${xy[1]}" r="14" style="color:${p.id===id?'#b66850':'#c9a66c'}"></circle><circle class="hotspot" cx="${xy[0]}" cy="${xy[1]}" r="7" fill="${p.id===id?'#b66850':'#c9a66c'}"></circle></g>`});svg.innerHTML=h}else svg.innerHTML=`<rect width="960" height="500" fill="transparent"/><g fill="rgba(220,210,190,.18)"><ellipse cx="210" cy="190" rx="120" ry="75"/><ellipse cx="300" cy="315" rx="62" ry="112"/><ellipse cx="555" cy="195" rx="170" ry="90"/><ellipse cx="610" cy="320" rx="72" ry="105"/><ellipse cx="820" cy="360" rx="72" ry="48"/></g>`}
function renderMapContext(){$('#mapContext').innerHTML=spots(P().id).map(p=>`<button data-point-link="${p.id}">${esc(l(p.title))}</button>`).join('')}
function renderSearch(q=''){const n=q.trim().toLowerCase(),seen=new Set(),a=D.TIMEPOINTS.map(p=>({id:eventForPoint(p.id),t:l(p.title),m:l(p.label)})).filter(x=>{if(seen.has(x.id))return false;seen.add(x.id);return!n||(`${x.t} ${x.m}`).toLowerCase().includes(n)}).slice(0,12);$('#searchResults').innerHTML=a.map(x=>`<button class="search-result" data-event="${esc(x.id)}"><small>${esc(x.m)}</small><strong>${esc(x.t)}</strong></button>`).join('')}
function renderAll(){renderDial();renderFilters();renderTimeline();renderLibrary();renderMyMap();renderSettings();renderMap(P().id,'timelineWorldMap');renderMapContext()}
function closeSheets(){$('#searchOverlay').hidden=true;$('#menuOverlay').hidden=true}
function openGuide(step=0){guideStep=step;$('#guideOverlay').hidden=false;renderGuide()}
function renderGuide(){const a=T().guideSteps,x=a[guideStep];$('#guideProgress').textContent=`${guideStep+1} / ${a.length}`;$('#guideIcon').textContent=x[0];$('#guideStepTitle').textContent=x[1];$('#guideStepCopy').textContent=x[2];$('#guideNext').textContent=guideStep===a.length-1?(lang==='ja'?'はじめる':'Начать'):(lang==='ja'?'次へ':'Далее');$('#guideSkip').textContent=lang==='ja'?'スキップ':'Пропустить'}
function finishGuide(){$('#guideOverlay').hidden=true;st.s('chronos-guide-v2','seen');go('home')}
document.addEventListener('click',e=>{const b=e.target.closest('button,[data-map-point]');if(!b)return;if(b.dataset.route){go(b.dataset.route);return}if(b.dataset.event){closeSheets();go('event/'+b.dataset.event);return}if(b.dataset.dialIndex!==undefined){setIndex(+b.dataset.dialIndex);go('timeline');return}if(b.dataset.pointIndex!==undefined){const i=+b.dataset.pointIndex;setIndex(i);go('event/'+eventForPoint(P().id));return}if(b.dataset.timelineFilter){filter=b.dataset.timelineFilter;renderFilters();renderTimeline();return}if(b.dataset.libraryFilter){libFilter=b.dataset.libraryFilter;renderFilters();renderLibrary();return}if(b.dataset.meaning){renderMeaning(E(route().id),b.dataset.meaning);return}if(b.dataset.langChoice){setLang(b.dataset.langChoice);return}if(b.dataset.themeChoice){setTheme(b.dataset.themeChoice);return}if(b.dataset.depthChoice){setDepth(b.dataset.depthChoice);return}if(b.dataset.pointLink||b.dataset.mapPoint){const p=pBy(b.dataset.pointLink||b.dataset.mapPoint);if(p){setIndex(D.TIMEPOINTS.indexOf(p));go('event/'+eventForPoint(p.id))}return}switch(b.dataset.action){case'language':setLang(lang==='ja'?'ru':'ja');break;case'theme':setTheme(theme==='dark'?'light':'dark');break;case'search':$('#searchOverlay').hidden=false;renderSearch();setTimeout(()=>$('#searchInput').focus(),30);break;case'menu':$('#menuOverlay').hidden=false;break;case'close-overlay':closeSheets();break;case'back':history.length>1?history.back():go('timeline');break;case'prehuman':go('event/prehuman');break;case'submit-test':submitTest();break;case'toggle-evidence':$('#evidenceBody').hidden=!$('#evidenceBody').hidden;break;case'map-expand':$('#mapFullscreen').hidden=false;renderMap(P().id,'fullscreenWorldMap');break;case'map-close':$('#mapFullscreen').hidden=true;break;case'guide':closeSheets();openGuide(0);break;case'guide-next':guideStep<T().guideSteps.length-1?(guideStep++,renderGuide()):finishGuide();break;case'guide-skip':finishGuide();break}});
$('#homeTimeSlider').addEventListener('input',e=>{idx=Math.max(0,Math.min(D.TIMEPOINTS.length-1,Math.round(+e.target.value/10)));$('#dialYear').textContent=l(P().label);$('#dialEvent').textContent=l(P().title)});$('#homeTimeSlider').addEventListener('change',()=>{setIndex(idx);go('timeline')});$('#historyScroll').addEventListener('scroll',onScroll,{passive:true});$('#searchInput').addEventListener('input',e=>renderSearch(e.target.value));$('#eventTabs').addEventListener('click',e=>{const b=e.target.closest('[data-event-tab]');if(!b)return;$$('.event-tab').forEach(x=>x.classList.toggle('active',x===b));$$('.event-panel').forEach(x=>x.classList.toggle('active',x.dataset.eventPanel===b.dataset.eventTab))});addEventListener('hashchange',applyRoute);addEventListener('resize',()=>{renderTimeline();if(route().screen==='timeline')scrollToIndex(idx,false)},{passive:true});
setTheme(theme);renderText();renderAll();applyRoute();if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});if(st.g('chronos-guide-v2')!=='seen')setTimeout(()=>openGuide(0),650);
})();